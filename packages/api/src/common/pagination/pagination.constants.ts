/** Default 1-based page index for list APIs. */
export const PAGINATION_DEFAULT_PAGE = 1;

/** Default page size when the client omits `pageSize`. */
export const PAGINATION_DEFAULT_PAGE_SIZE = 10;

/** Hard cap for `pageSize` on all paginated list endpoints. */
export const PAGINATION_MAX_PAGE_SIZE = 50;
