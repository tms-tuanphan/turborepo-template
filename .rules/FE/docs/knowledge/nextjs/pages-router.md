# Pages Router (legacy)

**When to use:** Maintaining old code only — **do not** add new Pages Router routes in this monorepo.

**Repo pattern:** Greenfield work uses App Router under `apps/web/app/`.

**Migration:** Incremental route move → `app/` + feature module. Generator: [generate-feature](../../../skills/generators/generate-feature/SKILL.md) for new screens.
