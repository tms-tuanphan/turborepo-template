# Zustand

**When to use:** Cross-component client state (sidebar, UI prefs) not in URL or server.

**Repo pattern:** `features/*/stores/*-store.ts` or `shared/stores/`; keep private unless exported.

**Generator:** [generate-zustand-store](../../../skills/generators/generate-zustand-store/SKILL.md)
