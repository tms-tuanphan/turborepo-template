# XSS

**Repo pattern:** Avoid `dangerouslySetInnerHTML`; sanitize if required. React escapes text by default.

**User content:** Render as text nodes; rich text needs trusted pipeline.

**See also:** [fe-security.mdc](../../../rules/fe-security.mdc)
